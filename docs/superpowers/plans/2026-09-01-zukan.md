# でんしゃずかん(図鑑)機能 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** クイズで1発正解を電車ごとに5回集めると写真と説明が解禁される、アルバム見開き型の図鑑画面を追加する。

**Architecture:** 進捗は localStorage(`train-quiz-zukan`)に電車ID→1発正解回数の辞書として保存し、`src/logic/zukan.ts` が読み書きする。見開き構成(カテゴリ順・8枠/見開き・null埋め)は純関数 `src/logic/zukanAlbum.ts` が計算する。画面は既存のスクリーンレジストリパターン(`renderZukan(ctx)`)で `src/screens/zukan.ts` に実装し、ゲーム選択画面から遷移する。説明文は trains.json の各エントリに `description` フィールドとして持たせる。

**Tech Stack:** Vite + TypeScript(vanilla, ES2020 / iPad 第6世代 Safari 対象)、Vitest + happy-dom、localStorage、CSS(フレームワークなし)。

**Spec:** `docs/superpowers/specs/2026-09-01-zukan-design.md`

## Global Constraints

- 子ども向け文言はすべてひらがな(分かち書き)。漢字禁止。
- `description` は ひらがな中心・カタカナは外来語等の最小限・80文字以内・2〜3文。
- `UNLOCK_COUNT = 5`、localStorage キーは `train-quiz-zukan`。
- テストは `npm test`(vitest run)。既存 58 テストを壊さないこと。
- 型チェックは `npm run build`(tsc --noEmit を含む)。vitest は型検査しないので、型を変えたタスクでは必ず build も実行する。
- localStorage を使うテストは `beforeEach` で `localStorage.clear()` する(happy-dom の Storage シムは `tests/setup.ts` が用意済み)。
- コミットは日本語プレフィクスなしの Conventional Commits(例: `feat: ...`, `test: ...`)。
- 作業ブランチ: `feature/zukan`(worktree は実行スキルが用意する)。

---

### Task 1: 図鑑進捗ロジック(zukan.ts)

**Files:**
- Create: `src/logic/zukan.ts`
- Test: `tests/zukan.test.ts`

**Interfaces:**
- Consumes: `Train`(`src/logic/types.ts`)、localStorage
- Produces(後続タスクが使う):
  - `UNLOCK_COUNT: 5`
  - `STORAGE_KEY: 'train-quiz-zukan'`
  - `loadZukanCounts(): Record<string, number>`
  - `recordFirstTryCorrect(trainId: string): void`
  - `countFor(trainId: string): number`
  - `isUnlocked(trainId: string): boolean`
  - `unlockedCount(trains: Train[]): number`

- [ ] **Step 1: 失敗するテストを書く**

`tests/zukan.test.ts`:

```ts
import { describe, it, expect, beforeEach } from 'vitest';
import {
  loadZukanCounts,
  recordFirstTryCorrect,
  countFor,
  isUnlocked,
  unlockedCount,
  UNLOCK_COUNT,
  STORAGE_KEY,
} from '../src/logic/zukan';
import type { Train } from '../src/logic/types';

function train(id: string): Train {
  return {
    id,
    name: { hiragana: 'てすと', normal: 'テスト' },
    category: 'local',
    image: `images/trains/${id}.webp`,
  };
}

beforeEach(() => localStorage.clear());

describe('図鑑の進捗', () => {
  it('未記録の電車は0回・未解禁', () => {
    expect(countFor('e5-hayabusa')).toBe(0);
    expect(isUnlocked('e5-hayabusa')).toBe(false);
  });

  it('1発正解を記録すると該当の電車だけ1ずつ増える', () => {
    recordFirstTryCorrect('e5-hayabusa');
    recordFirstTryCorrect('e5-hayabusa');
    expect(countFor('e5-hayabusa')).toBe(2);
    expect(countFor('komachi')).toBe(0);
  });

  it('UNLOCK_COUNT回で解禁され、以降も数え続ける', () => {
    for (let i = 0; i < UNLOCK_COUNT; i++) recordFirstTryCorrect('e5-hayabusa');
    expect(isUnlocked('e5-hayabusa')).toBe(true);
    recordFirstTryCorrect('e5-hayabusa');
    expect(countFor('e5-hayabusa')).toBe(UNLOCK_COUNT + 1);
  });

  it('localStorageに永続化される', () => {
    recordFirstTryCorrect('e5-hayabusa');
    expect(JSON.parse(localStorage.getItem(STORAGE_KEY)!)).toEqual({ 'e5-hayabusa': 1 });
  });

  it('壊れたJSONは空として扱い、記録も継続できる', () => {
    localStorage.setItem(STORAGE_KEY, '{oops');
    expect(loadZukanCounts()).toEqual({});
    recordFirstTryCorrect('a');
    expect(countFor('a')).toBe(1);
  });

  it('型が不正な値は捨て、小数は切り捨てる', () => {
    localStorage.setItem(STORAGE_KEY, JSON.stringify({ a: 'x', b: -2, c: 3.7, d: 2 }));
    expect(loadZukanCounts()).toEqual({ c: 3, d: 2 });
  });

  it('配列が保存されていても空として扱う', () => {
    localStorage.setItem(STORAGE_KEY, '[1,2]');
    expect(loadZukanCounts()).toEqual({});
  });

  it('unlockedCount: 解禁済みの台数を数える', () => {
    const trains = [train('a'), train('b'), train('c')];
    for (let i = 0; i < UNLOCK_COUNT; i++) recordFirstTryCorrect('a');
    for (let i = 0; i < UNLOCK_COUNT - 1; i++) recordFirstTryCorrect('b');
    expect(unlockedCount(trains)).toBe(1);
  });
});
```

- [ ] **Step 2: 失敗を確認する**

Run: `npx vitest run tests/zukan.test.ts`
Expected: FAIL(モジュールが存在しない)

- [ ] **Step 3: 実装する**

`src/logic/zukan.ts`:

```ts
import type { Train } from './types';

/** この回数の1発正解で図鑑に登録される */
export const UNLOCK_COUNT = 5;
export const STORAGE_KEY = 'train-quiz-zukan';

type Counts = Record<string, number>;

export function loadZukanCounts(): Counts {
  try {
    const raw = localStorage.getItem(STORAGE_KEY);
    if (!raw) return {};
    const data: unknown = JSON.parse(raw);
    if (typeof data !== 'object' || data === null || Array.isArray(data)) return {};
    const counts: Counts = {};
    for (const [id, n] of Object.entries(data)) {
      if (typeof n === 'number' && Number.isFinite(n) && n > 0) counts[id] = Math.floor(n);
    }
    return counts;
  } catch {
    return {};
  }
}

function save(counts: Counts): void {
  try {
    localStorage.setItem(STORAGE_KEY, JSON.stringify(counts));
  } catch {
    // 保存失敗(容量超過など)は無視。図鑑は進まないがアプリは止めない。
  }
}

export function recordFirstTryCorrect(trainId: string): void {
  const counts = loadZukanCounts();
  counts[trainId] = (counts[trainId] ?? 0) + 1;
  save(counts);
}

export function countFor(trainId: string): number {
  return loadZukanCounts()[trainId] ?? 0;
}

export function isUnlocked(trainId: string): boolean {
  return countFor(trainId) >= UNLOCK_COUNT;
}

export function unlockedCount(trains: Train[]): number {
  const counts = loadZukanCounts();
  return trains.filter((t) => (counts[t.id] ?? 0) >= UNLOCK_COUNT).length;
}
```

- [ ] **Step 4: テストが通ることを確認する**

Run: `npx vitest run tests/zukan.test.ts`
Expected: PASS(8件)

- [ ] **Step 5: 全体テストとコミット**

Run: `npm test`
Expected: 既存含め全件 PASS

```bash
git add src/logic/zukan.ts tests/zukan.test.ts
git commit -m "feat: add zukan progress tracking (first-try correct counts)"
```

---

### Task 2: 見開き構成ロジック(zukanAlbum.ts)

**Files:**
- Create: `src/logic/zukanAlbum.ts`
- Test: `tests/zukanAlbum.test.ts`

**Interfaces:**
- Consumes: `Train`, `Category`(`src/logic/types.ts`)
- Produces(後続タスクが使う):
  - `SLOTS_PER_SPREAD: 8`
  - `CATEGORY_ORDER: Category[]`(`['shinkansen', 'express', 'local']`)
  - `type Slot = Train | null`
  - `interface Spread { left: Slot[]; right: Slot[] }`(各長さ4)
  - `interface Album { spreads: Spread[]; categoryStart: Record<Category, number> }`
  - `buildAlbum(trains: Train[]): Album`

- [ ] **Step 1: 失敗するテストを書く**

`tests/zukanAlbum.test.ts`:

```ts
import { describe, it, expect } from 'vitest';
import { buildAlbum, SLOTS_PER_SPREAD } from '../src/logic/zukanAlbum';
import trainsJson from '../src/data/trains.json';
import type { Train, Category } from '../src/logic/types';

const real = trainsJson as Train[];

function train(id: string, category: Category): Train {
  return {
    id,
    name: { hiragana: 'てすと', normal: 'テスト' },
    category,
    image: `images/trains/${id}.webp`,
  };
}

describe('buildAlbum', () => {
  it('実データ: 16見開き、カテゴリ先頭は 0 / 3 / 11', () => {
    const album = buildAlbum(real);
    expect(album.spreads).toHaveLength(16);
    expect(album.categoryStart).toEqual({ shinkansen: 0, express: 3, local: 11 });
  });

  it('実データ: 全119件がカテゴリ順(しんかんせん→とっきゅう→ふつう)で並ぶ', () => {
    const album = buildAlbum(real);
    const flat = album.spreads
      .flatMap((s) => [...s.left, ...s.right])
      .filter((t): t is Train => t !== null);
    expect(flat).toHaveLength(real.length);
    expect(flat.map((t) => t.id)).toEqual([
      ...real.filter((t) => t.category === 'shinkansen').map((t) => t.id),
      ...real.filter((t) => t.category === 'express').map((t) => t.id),
      ...real.filter((t) => t.category === 'local').map((t) => t.id),
    ]);
  });

  it('実データ: しんかんせん3見開き目は2件だけ入り、残りはnull埋め(18=8+8+2)', () => {
    const album = buildAlbum(real);
    const third = album.spreads[2];
    expect(third.left).toHaveLength(4);
    expect(third.right).toHaveLength(4);
    expect(third.left.filter(Boolean)).toHaveLength(2);
    expect(third.right.filter(Boolean)).toHaveLength(0);
  });

  it('合成データ: 9件は2見開き(8+1)、各カテゴリは新しい見開きから始まる', () => {
    const trains = [
      ...Array.from({ length: 9 }, (_, i) => train(`s${i}`, 'shinkansen')),
      train('e1', 'express'),
      train('l1', 'local'),
    ];
    const album = buildAlbum(trains);
    expect(album.spreads).toHaveLength(4);
    expect(album.categoryStart).toEqual({ shinkansen: 0, express: 2, local: 3 });
    expect(album.spreads[1].left.filter(Boolean)).toHaveLength(1);
    expect(album.spreads[2].left[0]!.id).toBe('e1');
  });

  it('SLOTS_PER_SPREAD は 8(2x2 x 2ページ)', () => {
    expect(SLOTS_PER_SPREAD).toBe(8);
  });
});
```

- [ ] **Step 2: 失敗を確認する**

Run: `npx vitest run tests/zukanAlbum.test.ts`
Expected: FAIL(モジュールが存在しない)

- [ ] **Step 3: 実装する**

`src/logic/zukanAlbum.ts`:

```ts
import type { Train, Category } from './types';

export const SLOTS_PER_SPREAD = 8;
export const CATEGORY_ORDER: Category[] = ['shinkansen', 'express', 'local'];

export type Slot = Train | null;

/** アルバムの見開き1つ。left/right とも長さ4(2x2) */
export interface Spread {
  left: Slot[];
  right: Slot[];
}

export interface Album {
  spreads: Spread[];
  /** 各カテゴリが始まる見開き番号 */
  categoryStart: Record<Category, number>;
}

export function buildAlbum(trains: Train[]): Album {
  const spreads: Spread[] = [];
  const categoryStart = {} as Record<Category, number>;
  for (const cat of CATEGORY_ORDER) {
    const members = trains.filter((t) => t.category === cat);
    categoryStart[cat] = spreads.length;
    for (let i = 0; i < members.length; i += SLOTS_PER_SPREAD) {
      const slots: Slot[] = members.slice(i, i + SLOTS_PER_SPREAD);
      while (slots.length < SLOTS_PER_SPREAD) slots.push(null);
      spreads.push({ left: slots.slice(0, 4), right: slots.slice(4, 8) });
    }
  }
  return { spreads, categoryStart };
}
```

- [ ] **Step 4: テストが通ることを確認する**

Run: `npx vitest run tests/zukanAlbum.test.ts`
Expected: PASS(5件)

- [ ] **Step 5: 全体テストとコミット**

Run: `npm test`

```bash
git add src/logic/zukanAlbum.ts tests/zukanAlbum.test.ts
git commit -m "feat: add zukan album spread layout logic"
```

---

### Task 3: description フィールド導入と119件の説明文執筆

**Files:**
- Modify: `src/logic/types.ts`(Train に description 追加)
- Modify: `src/logic/validate.ts`(検証ルール追加)
- Modify: `scripts/validate-data.mjs`(同上)
- Modify: `src/data/trains.json`(全119件に description 追加)
- Modify: `tests/data.test.ts`(fixture 更新+新ルールのテスト)
- Modify: `tests/question.test.ts`, `tests/zukan.test.ts`, `tests/zukanAlbum.test.ts` ほか
  Train リテラルを持つ全テスト(fixture に description 追加。tsc が漏れを列挙する)
- Test: `tests/data.test.ts`

**Interfaces:**
- Produces(後続タスクが使う): `Train.description: string`(必須)

- [ ] **Step 1: 型にフィールドを追加する**

`src/logic/types.ts` の `Train` に追加:

```ts
export interface Train {
  id: string;
  name: TrainName;
  category: Category;
  image: string;
  /** 図鑑用のせつめい(ひらがな中心・漢字なし・80文字以内・2〜3文) */
  description: string;
  credit?: Credit;
  lookalikes?: string[];
}
```

- [ ] **Step 2: 検証ルールのテストを書く(失敗確認)**

`tests/data.test.ts` の `validateTrains` describe 内に追加。既存の `base` fixture にも
`description: 'てすとの せつめいだよ。'` を追加する(型必須化のため)。

```ts
it('せつめいの欠落・漢字・長すぎを検出する', () => {
  expect(validateTrains([{ ...base, description: '' }])).toContain('せつめいなし: a');
  expect(validateTrains([{ ...base, description: '電車だよ。' }])).toContain(
    'せつめいに漢字: a',
  );
  expect(validateTrains([{ ...base, description: 'あ'.repeat(81) }])).toContain(
    'せつめいが長すぎ: a',
  );
  expect(validateTrains([base])).toEqual([]);
});
```

Run: `npx vitest run tests/data.test.ts`
Expected: 新テストが FAIL(ルール未実装)。さらに既存の
「データ検証エラーがない」も、この時点では trains.json に description が
ないため後続 Step で FAIL する(それで正しい)。

- [ ] **Step 3: 検証ルールを実装する**

`src/logic/validate.ts` のループ内に追加:

```ts
const KANJI = /[一-鿿]/;
// for ループ内:
if (!t.description) errors.push(`せつめいなし: ${t.id}`);
else if (KANJI.test(t.description)) errors.push(`せつめいに漢字: ${t.id}`);
else if (t.description.length > 80) errors.push(`せつめいが長すぎ: ${t.id}`);
```

`scripts/validate-data.mjs` のループ内にも同等を追加:

```js
if (!t.description || /[一-鿿]/.test(t.description) || t.description.length > 80)
  errors.push(`せつめい不備: ${t.id}`);
```

- [ ] **Step 4: 119件の説明文を書いて trains.json にマージする**

スクラッチパッドに一時スクリプト(例 `add-descriptions.mjs`)を作り、
id→説明文の完全な辞書を書いてマージする。**全119 id を網羅**(欠けがあれば throw):

```js
import { readFileSync, writeFileSync } from 'node:fs';
const DESC = {
  'h5-hayabusa': 'しんあおもりから しんはこだてほくとまで はしる みどりの しんかんせん。ほっかいどうの なかまだよ。',
  'e5-hayabusa': 'とうきょうから しんあおもりまで はしる みどりの しんかんせん。じそく320キロで にほんで いちばん はやいよ。',
  // ...全119件...
};
const trains = JSON.parse(readFileSync('src/data/trains.json', 'utf8'));
for (const t of trains) {
  if (!DESC[t.id]) throw new Error(`missing description: ${t.id}`);
  t.description = DESC[t.id];
}
writeFileSync('src/data/trains.json', JSON.stringify(trains, null, 2) + '\n');
console.log(`OK: ${trains.length}件`);
```

**執筆ルール:**
- ひらがな分かち書き、2〜3文、80文字以内、漢字禁止。カタカナは外来語・固有名詞のみ
  (例: ドクターイエロー、ラッキー、キロ)。数字は算用数字可。
- 内容: ①どこからどこまで走るか(または どの地域を走るか) ②色・形・速さの特徴
  ③あれば まめちしき。語尾は「〜よ」「〜だよ」でやさしく。
- 事実に自信がない区間は「〜の あたりを はしるよ」のような確実な表現にする。
  引退済みの列車(例: スーパービューおどりこ)は「まえに はしっていたよ」と書く。
- 例:
  - あずさ: `しんじゅくから まつもとまで はしる とっきゅう。むらさきいろの ラインが めじるしだよ。`
  - やまのてせん: `とうきょうの まちを ぐるぐる まわる みどりの でんしゃ。ひとまわり やく1じかんだよ。`
  - ソニック: `はかたから おおいたまで はしる あおい とっきゅう。みみみたいな かざりが ついているよ。`
  - ドクターイエロー: `きいろい けんさようの しんかんせん。せんろが あんぜんか しらべながら はしるよ。みられたら ラッキーだよ。`

実行後、一時スクリプトは削除する。

- [ ] **Step 5: 検証・型・テストを通す**

Run: `npm run validate`
Expected: `OK`(せつめい不備 0件)

Run: `npm run build`
Expected: 型エラー 0件。tsconfig は `tests` も型検査対象なので、`description` 欠落の
Train リテラルが残っていれば tsc が列挙する。`description: 'てすとの せつめいだよ。'` を
足して回る(`tests/question.test.ts`・`tests/zukan.test.ts`・`tests/zukanAlbum.test.ts` の
`train()` ヘルパ、`tests/data.test.ts` の `base`、その他 tsc が指摘した箇所すべて)。

Run: `npm test`
Expected: 全件 PASS(「データ検証エラーがない」含む)

- [ ] **Step 6: コミット**

```bash
git add src/logic/types.ts src/logic/validate.ts scripts/validate-data.mjs src/data/trains.json
git add -u tests/
git commit -m "feat: add hiragana descriptions to all 119 trains"
```

---

### Task 4: 1発正解フック(question.ts)

**Files:**
- Modify: `src/screens/question.ts`
- Test: `tests/question.test.ts`

**Interfaces:**
- Consumes: `recordFirstTryCorrect`, `countFor`(Task 1)、`SessionState.failedThisQuestion`(既存)
- Produces: 正解演出の前に図鑑カウントが記録される副作用(画面変更なし)

- [ ] **Step 1: 失敗するテストを書く**

`tests/question.test.ts` に追加(import に `countFor` を足す。
`beforeEach` に `localStorage.clear()` を追加する):

```ts
import { countFor } from '../src/logic/zukan';

// beforeEach を以下に変更:
beforeEach(() => {
  vi.useFakeTimers();
  localStorage.clear();
});

it('1発正解で図鑑カウントが1増える', () => {
  const ctx = fixtureCtx(2);
  renderQuestion(ctx);
  const q = ctx.session!.questions[0];
  const buttons = [...ctx.root.querySelectorAll<HTMLButtonElement>('.choice')];
  buttons[q.correctIndex].click();
  expect(countFor(q.train.id)).toBe(1);
});

it('まちがえたあとの正解では図鑑カウントが増えない', () => {
  const ctx = fixtureCtx(2);
  renderQuestion(ctx);
  const q = ctx.session!.questions[0];
  const buttons = [...ctx.root.querySelectorAll<HTMLButtonElement>('.choice')];
  buttons[q.correctIndex === 0 ? 1 : 0].click();
  vi.advanceTimersByTime(1500);
  buttons[q.correctIndex].click();
  expect(countFor(q.train.id)).toBe(0);
});
```

- [ ] **Step 2: 失敗を確認する**

Run: `npx vitest run tests/question.test.ts`
Expected: 新2件が FAIL

- [ ] **Step 3: 実装する**

`src/screens/question.ts` — import を追加し、クリックハンドラ冒頭を変更:

```ts
import { recordFirstTryCorrect } from '../logic/zukan';

// ハンドラ内。answer() は正解時に failedThisQuestion をリセットするため、
// 呼び出し前に1発かどうかを控える。
btn.addEventListener('click', () => {
  const firstTry = !session.failedThisQuestion;
  const result = answer(session, Number(btn.dataset.index));
  for (const b of buttons) b.disabled = true;
  conductor.hidden = false;
  if (result === 'correct') {
    if (firstTry) recordFirstTryCorrect(q.train.id);
    // ...既存の正解処理はそのまま...
```

- [ ] **Step 4: テストが通ることを確認する**

Run: `npx vitest run tests/question.test.ts`
Expected: 全件 PASS

- [ ] **Step 5: 全体テストとコミット**

Run: `npm test`

```bash
git add src/screens/question.ts tests/question.test.ts
git commit -m "feat: record first-try correct answers for zukan"
```

---

### Task 5: 図鑑画面の骨格と配線(zukan スクリーン+入口+ロック)

**Files:**
- Create: `src/screens/zukan.ts`
- Modify: `src/app.ts`(ScreenName・screens・LOCKABLE)
- Modify: `src/screens/gameSelect.ts`(「でんしゃずかん」カード)
- Modify: `src/styles.css`(最低限: `.zukan-detail` は既存 `.overlay` を使うので不要。ここでは追加なしでよい — 見た目は Task 7)
- Test: `tests/zukanScreen.test.ts`, `tests/lock.test.ts`

**Interfaces:**
- Consumes: `buildAlbum`/`CATEGORY_ORDER`/`Slot`(Task 2)、`loadZukanCounts`/`UNLOCK_COUNT`/`unlockedCount`(Task 1)、`Train.description`(Task 3)、`asset()`(既存 `src/ui/asset.ts`)
- Produces: `renderZukan(ctx: AppContext): void`、ScreenName `'zukan'`。
  DOM クラス(Task 6/7 が依存): `.screen-zukan` `.zukan-album` `.zukan-page-left`
  `.zukan-page-right` `.zukan-tabs` `.zukan-tab`(data-cat)`.zukan-prev` `.zukan-next`
  `.zukan-counter` `.zukan-slot` `.zukan-unlocked`(data-train)`.zukan-locked`
  `.zukan-empty` `.zukan-photo` `.zukan-name` `.zukan-stamps` `.stamp`(.on)
  `.zukan-detail`(hidden オーバーレイ)

- [ ] **Step 1: 失敗するテストを書く**

`tests/zukanScreen.test.ts`:

```ts
import { describe, it, expect, vi, beforeEach } from 'vitest';
import { renderZukan } from '../src/screens/zukan';
import { createApp } from '../src/app';
import { recordFirstTryCorrect, UNLOCK_COUNT } from '../src/logic/zukan';
import type { AppContext } from '../src/app';
import type { Train, Category } from '../src/logic/types';

function train(id: string, category: Category): Train {
  return {
    id,
    name: { hiragana: `でんしゃ${id}`, normal: `電車${id}` },
    category,
    image: `images/trains/${id}.webp`,
    description: `${id}の せつめいだよ。`,
  };
}

// しんかんせん2 / とっきゅう9 / ふつう1 → 見開き4(0 / 1,2 / 3)
function fixtureTrains(): Train[] {
  return [
    train('s1', 'shinkansen'),
    train('s2', 'shinkansen'),
    ...Array.from({ length: 9 }, (_, i) => train(`e${i + 1}`, 'express')),
    train('l1', 'local'),
  ];
}

function fixtureCtx(): AppContext {
  const ctx = {
    root: document.createElement('div'),
    trains: fixtureTrains(),
    modes: [],
    settings: { notation: 'hiragana', questionCount: 5, sound: false, adultMode: false },
    audio: { play: vi.fn(), stop: vi.fn() },
    currentMode: null,
    session: null,
    navigate: vi.fn(),
  } as unknown as AppContext;
  document.body.appendChild(ctx.root);
  return ctx;
}

beforeEach(() => {
  localStorage.clear();
  document.body.innerHTML = '';
});

describe('renderZukan: 見開きの表示', () => {
  it('1見開き目に左右4枠ずつ描画され、余りは空き枠になる', () => {
    const ctx = fixtureCtx();
    renderZukan(ctx);
    const leftSlots = ctx.root.querySelectorAll('.zukan-page-left .zukan-slot');
    const rightSlots = ctx.root.querySelectorAll('.zukan-page-right .zukan-slot');
    expect(leftSlots).toHaveLength(4);
    expect(rightSlots).toHaveLength(4);
    // しんかんせんは2件: 左ページに2 + 空き2、右ページは空き4
    expect(ctx.root.querySelectorAll('.zukan-page-left .zukan-empty')).toHaveLength(2);
    expect(ctx.root.querySelectorAll('.zukan-page-right .zukan-empty')).toHaveLength(4);
  });

  it('未解禁: ぼかし写真+スタンプが出て、集めた数だけ点灯する', () => {
    recordFirstTryCorrect('s1');
    recordFirstTryCorrect('s1');
    const ctx = fixtureCtx();
    renderZukan(ctx);
    const locked = ctx.root.querySelector('.zukan-locked')!;
    expect(locked.querySelector('.zukan-photo img')).not.toBeNull();
    expect(locked.querySelectorAll('.stamp')).toHaveLength(UNLOCK_COUNT);
    expect(locked.querySelectorAll('.stamp.on')).toHaveLength(2);
    expect(locked.textContent).not.toContain('でんしゃs1');
  });

  it('解禁済み: 写真と なまえ が出る', () => {
    for (let i = 0; i < UNLOCK_COUNT; i++) recordFirstTryCorrect('s1');
    const ctx = fixtureCtx();
    renderZukan(ctx);
    const unlocked = ctx.root.querySelector<HTMLElement>('.zukan-unlocked')!;
    expect(unlocked.dataset.train).toBe('s1');
    expect(unlocked.textContent).toContain('でんしゃs1');
    expect(unlocked.querySelectorAll('.stamp')).toHaveLength(0);
  });

  it('カウンター: あつめた n/全数 が出る', () => {
    for (let i = 0; i < UNLOCK_COUNT; i++) recordFirstTryCorrect('s1');
    const ctx = fixtureCtx();
    renderZukan(ctx);
    expect(ctx.root.querySelector('.zukan-counter')!.textContent).toContain('あつめた 1/12');
  });

  it('タブが3つあり、もどるで gameSelect へ', () => {
    const ctx = fixtureCtx();
    renderZukan(ctx);
    expect(ctx.root.querySelectorAll('.zukan-tab')).toHaveLength(3);
    ctx.root.querySelector<HTMLButtonElement>('[data-action=back]')!.click();
    expect(ctx.navigate).toHaveBeenCalledWith('gameSelect');
  });
});

describe('図鑑への入口と登録', () => {
  it('ゲーム選択画面の「でんしゃずかん」カードで zukan へ遷移する', () => {
    document.body.innerHTML = '<div id="app"></div>';
    const app = createApp(document.querySelector<HTMLElement>('#app')!);
    app.navigate('gameSelect');
    document.querySelector<HTMLButtonElement>('[data-game=zukan]')!.click();
    expect(document.querySelector('.screen-zukan')).not.toBeNull();
  });
});
```

`tests/lock.test.ts` に追加:

```ts
it('制限到達後は図鑑もロック画面になる', () => {
  addPlayTime(DAILY_LIMIT_SECONDS);
  const app = createApp(root());
  app.navigate('zukan');
  expect(root().querySelector('.screen-locked')).not.toBeNull();
});
```

- [ ] **Step 2: 失敗を確認する**

Run: `npx vitest run tests/zukanScreen.test.ts tests/lock.test.ts`
Expected: FAIL(renderZukan 未実装、'zukan' 未登録)

- [ ] **Step 3: 画面を実装する**

`src/screens/zukan.ts`:

```ts
import type { AppContext } from '../app';
import type { Category, Train } from '../logic/types';
import { asset } from '../ui/asset';
import { buildAlbum, CATEGORY_ORDER, type Slot } from '../logic/zukanAlbum';
import { loadZukanCounts, UNLOCK_COUNT, unlockedCount } from '../logic/zukan';

const TAB_LABELS: Record<Category, string> = {
  shinkansen: 'しんかんせん',
  express: 'とっきゅう',
  local: 'ふつうでんしゃ',
};

export function renderZukan(ctx: AppContext): void {
  const album = buildAlbum(ctx.trains);
  const counts = loadZukanCounts();
  let current = 0;

  ctx.root.innerHTML = `
    <section class="screen screen-zukan">
      <div class="zukan-album"
           style="background-image:url(${asset('images/zukan/album.webp')})">
        <div class="zukan-page zukan-page-left"></div>
        <div class="zukan-page zukan-page-right"></div>
        <div class="zukan-tabs">
          ${CATEGORY_ORDER.map(
            (c) => `<button class="zukan-tab" data-cat="${c}">${TAB_LABELS[c]}</button>`,
          ).join('')}
        </div>
      </div>
      <button class="zukan-nav zukan-prev" aria-label="まえのページ">◀</button>
      <button class="zukan-nav zukan-next" aria-label="つぎのページ">▶</button>
      <div class="zukan-counter">あつめた ${unlockedCount(ctx.trains)}/${ctx.trains.length}</div>
      <button class="btn btn-back" data-action="back">もどる</button>
      <div class="overlay zukan-detail" hidden>
        <figure class="photo-card"><img class="zukan-detail-photo" src="" alt=""></figure>
        <div class="zukan-detail-name"></div>
        <div class="zukan-detail-formal"></div>
        <p class="zukan-detail-desc"></p>
        <button class="btn" data-action="close">とじる</button>
      </div>
    </section>`;

  const albumEl = ctx.root.querySelector<HTMLElement>('.zukan-album')!;
  const left = ctx.root.querySelector<HTMLElement>('.zukan-page-left')!;
  const right = ctx.root.querySelector<HTMLElement>('.zukan-page-right')!;
  const prevBtn = ctx.root.querySelector<HTMLButtonElement>('.zukan-prev')!;
  const nextBtn = ctx.root.querySelector<HTMLButtonElement>('.zukan-next')!;
  const tabs = [...ctx.root.querySelectorAll<HTMLButtonElement>('.zukan-tab')];
  const detail = ctx.root.querySelector<HTMLElement>('.zukan-detail')!;

  function slotHtml(slot: Slot): string {
    if (!slot) return '<div class="zukan-slot zukan-empty"></div>';
    const count = counts[slot.id] ?? 0;
    if (count >= UNLOCK_COUNT) {
      return `
        <button class="zukan-slot zukan-unlocked" data-train="${slot.id}">
          <span class="zukan-photo"><img src="${asset(slot.image)}" alt=""></span>
          <span class="zukan-name">${slot.name.hiragana}</span>
        </button>`;
    }
    return `
      <div class="zukan-slot zukan-locked">
        <span class="zukan-photo"><img src="${asset(slot.image)}" alt=""></span>
        <span class="zukan-stamps">${Array.from(
          { length: UNLOCK_COUNT },
          (_, i) => `<span class="stamp${i < count ? ' on' : ''}">★</span>`,
        ).join('')}</span>
      </div>`;
  }

  function currentCategory(): Category {
    let cat: Category = CATEGORY_ORDER[0];
    for (const c of CATEGORY_ORDER) {
      if (album.categoryStart[c] <= current) cat = c;
    }
    return cat;
  }

  function renderSpread(): void {
    const spread = album.spreads[current];
    left.innerHTML = spread.left.map(slotHtml).join('');
    right.innerHTML = spread.right.map(slotHtml).join('');
    prevBtn.disabled = current === 0;
    nextBtn.disabled = current === album.spreads.length - 1;
    const cat = currentCategory();
    for (const tab of tabs) tab.classList.toggle('active', tab.dataset.cat === cat);
    for (const el of ctx.root.querySelectorAll<HTMLButtonElement>('.zukan-unlocked')) {
      el.addEventListener('click', () => openDetail(el.dataset.train!));
    }
  }

  function openDetail(id: string): void {
    const t = ctx.trains.find((tr) => tr.id === id)!;
    detail.querySelector<HTMLImageElement>('.zukan-detail-photo')!.src = asset(t.image);
    detail.querySelector<HTMLElement>('.zukan-detail-name')!.textContent = t.name.hiragana;
    detail.querySelector<HTMLElement>('.zukan-detail-formal')!.textContent = t.name.normal;
    detail.querySelector<HTMLElement>('.zukan-detail-desc')!.textContent = t.description;
    ctx.audio.play('tap');
    detail.hidden = false;
  }

  // ページ送り・タブ・とじる(Task 6 でテストする対話も配線はここで済ませる)
  function turnTo(index: number): void {
    ctx.audio.play('tap');
    current = index;
    albumEl.classList.remove('zukan-turning');
    void albumEl.offsetWidth; // アニメ再トリガー用の reflow
    albumEl.classList.add('zukan-turning');
    renderSpread();
  }
  prevBtn.addEventListener('click', () => {
    if (current > 0) turnTo(current - 1);
  });
  nextBtn.addEventListener('click', () => {
    if (current < album.spreads.length - 1) turnTo(current + 1);
  });
  for (const tab of tabs) {
    tab.addEventListener('click', () => turnTo(album.categoryStart[tab.dataset.cat as Category]));
  }
  detail.querySelector<HTMLButtonElement>('[data-action=close]')!.addEventListener('click', () => {
    ctx.audio.play('tap');
    detail.hidden = true;
  });
  ctx.root.querySelector<HTMLButtonElement>('[data-action=back]')!.addEventListener('click', () => {
    ctx.audio.play('tap');
    ctx.navigate('gameSelect');
  });

  renderSpread();
}
```

`src/app.ts` — 3か所変更:

```ts
import { renderZukan } from './screens/zukan';

export type ScreenName =
  | 'start'
  | 'gameSelect'
  | 'modeSelect'
  | 'departure'
  | 'question'
  | 'interlude'
  | 'arrival'
  | 'result'
  | 'settings'
  | 'locked'
  | 'zukan';

const LOCKABLE: ReadonlySet<ScreenName> = new Set([
  'start',
  'gameSelect',
  'modeSelect',
  'departure',
  'zukan',
]);

export const screens: Partial<Record<ScreenName, ScreenRenderer>> = {
  // ...既存...
  zukan: renderZukan,
};
```

`src/screens/gameSelect.ts` — GAMES を2件にし、ハンドラをループ化:

```ts
import type { AppContext, ScreenName } from '../app';
import { asset } from '../ui/asset';

const GAMES: { id: string; label: string; icon: string; screen: ScreenName }[] = [
  {
    id: 'train-quiz',
    label: 'でんしゃクイズ',
    icon: 'images/hero/shinkansen.webp',
    screen: 'modeSelect',
  },
  { id: 'zukan', label: 'でんしゃずかん', icon: 'images/zukan/cover.webp', screen: 'zukan' },
];

export function renderGameSelect(ctx: AppContext): void {
  ctx.root.innerHTML = `
    <section class="screen screen-game-select">
      <h2 class="heading">どれで あそぶ?</h2>
      <div class="card-grid">
        ${GAMES.map(
          (g) => `
          <button class="btn game-card" data-game="${g.id}">
            <img src="${asset(g.icon)}" alt="" onerror="this.hidden=true">
            <span>${g.label}</span>
          </button>`,
        ).join('')}
      </div>
      <button class="btn btn-back" data-action="back">もどる</button>
    </section>`;
  for (const g of GAMES) {
    ctx.root.querySelector<HTMLButtonElement>(`[data-game=${g.id}]`)!.addEventListener(
      'click',
      () => {
        ctx.audio.play('tap');
        ctx.navigate(g.screen);
      },
    );
  }
  ctx.root.querySelector<HTMLButtonElement>('[data-action=back]')!.addEventListener('click', () => {
    ctx.audio.play('tap');
    ctx.navigate('start');
  });
}
```

※ `images/zukan/cover.webp` はユーザー画像の受領前は存在しない。
`onerror="this.hidden=true"` でアイコンなしでも崩れない(既存の車掌画像と同じパターン)。

- [ ] **Step 4: テストが通ることを確認する**

Run: `npx vitest run tests/zukanScreen.test.ts tests/lock.test.ts`
Expected: 全件 PASS

- [ ] **Step 5: 全体テスト・型チェック・コミット**

Run: `npm test && npm run build`

```bash
git add src/screens/zukan.ts src/app.ts src/screens/gameSelect.ts tests/zukanScreen.test.ts tests/lock.test.ts
git commit -m "feat: add zukan album screen with entry card and daily-limit lock"
```

---

### Task 6: 図鑑のインタラクション検証(ページ送り・タブ・拡大カード)

配線は Task 5 で済んでいる。このタスクは**対話仕様をテストで固定する**
(リグレッション防止のゲート)。

**Files:**
- Test: `tests/zukanScreen.test.ts`(describe 追加)
- Modify: `src/screens/zukan.ts`(テストで不具合が出た場合のみ)

**Interfaces:**
- Consumes: Task 5 の DOM 構造(fixtureCtx: しんかんせん2/とっきゅう9/ふつう1 → 見開き4)

- [ ] **Step 1: 対話テストを書く**

`tests/zukanScreen.test.ts` に追加:

```ts
describe('renderZukan: ページ送りとタブ', () => {
  it('先頭では◀が無効、▶で次の見開きへ進む', () => {
    const ctx = fixtureCtx();
    renderZukan(ctx);
    const prev = ctx.root.querySelector<HTMLButtonElement>('.zukan-prev')!;
    const next = ctx.root.querySelector<HTMLButtonElement>('.zukan-next')!;
    expect(prev.disabled).toBe(true);
    next.click();
    // 見開き2つ目 = とっきゅう e1..e8
    expect(ctx.root.querySelector<HTMLElement>('.zukan-locked .zukan-photo img')!.src).toContain(
      'e1.webp',
    );
    expect(prev.disabled).toBe(false);
    expect(ctx.audio.play).toHaveBeenCalledWith('tap');
  });

  it('最終見開きでは▶が無効', () => {
    const ctx = fixtureCtx();
    renderZukan(ctx);
    const next = ctx.root.querySelector<HTMLButtonElement>('.zukan-next')!;
    next.click();
    next.click();
    next.click();
    expect(next.disabled).toBe(true);
  });

  it('タブでカテゴリ先頭の見開きへ飛び、タブが強調される', () => {
    const ctx = fixtureCtx();
    renderZukan(ctx);
    ctx.root.querySelector<HTMLButtonElement>('[data-cat=local]')!.click();
    expect(ctx.root.querySelector<HTMLElement>('.zukan-locked .zukan-photo img')!.src).toContain(
      'l1.webp',
    );
    expect(
      ctx.root.querySelector<HTMLElement>('[data-cat=local]')!.classList.contains('active'),
    ).toBe(true);
    expect(
      ctx.root.querySelector<HTMLElement>('[data-cat=shinkansen]')!.classList.contains('active'),
    ).toBe(false);
  });
});

describe('renderZukan: 拡大カード', () => {
  it('解禁済み写真タップで せつめい が開き、とじるで閉じる', () => {
    for (let i = 0; i < UNLOCK_COUNT; i++) recordFirstTryCorrect('s1');
    const ctx = fixtureCtx();
    renderZukan(ctx);
    ctx.root.querySelector<HTMLButtonElement>('.zukan-unlocked')!.click();
    const detail = ctx.root.querySelector<HTMLElement>('.zukan-detail')!;
    expect(detail.hidden).toBe(false);
    expect(detail.querySelector('.zukan-detail-name')!.textContent).toBe('でんしゃs1');
    expect(detail.querySelector('.zukan-detail-formal')!.textContent).toBe('電車s1');
    expect(detail.querySelector('.zukan-detail-desc')!.textContent).toBe('s1の せつめいだよ。');
    detail.querySelector<HTMLButtonElement>('[data-action=close]')!.click();
    expect(detail.hidden).toBe(true);
  });

  it('未解禁スロットをタップしても何も開かない', () => {
    const ctx = fixtureCtx();
    renderZukan(ctx);
    (ctx.root.querySelector<HTMLElement>('.zukan-locked')!).click();
    expect(ctx.root.querySelector<HTMLElement>('.zukan-detail')!.hidden).toBe(true);
  });
});
```

- [ ] **Step 2: テストを実行する**

Run: `npx vitest run tests/zukanScreen.test.ts`
Expected: PASS(Task 5 の実装で通るはず。落ちたら zukan.ts を修正して通す)

- [ ] **Step 3: 全体テストとコミット**

Run: `npm test`

```bash
git add tests/zukanScreen.test.ts src/screens/zukan.ts
git commit -m "test: cover zukan paging, tabs, and detail card interactions"
```

---

### Task 7: アルバムの見た目(CSS)とビジュアル確認

**Files:**
- Modify: `src/styles.css`(末尾に図鑑セクションを追加)
- Test: 既存テストのリグレッションなし+目視(スクリーンショット)

**Interfaces:**
- Consumes: Task 5 の DOM クラス群
- Produces: 図鑑画面のスタイル一式(画像未受領時は CSS フォールバックの
  クリーム色ページ+とじ目影で成立する)

- [ ] **Step 1: スタイルを追加する**

`src/styles.css` の末尾に追加:

```css
/* ---- でんしゃずかん ---- */
.screen-zukan {
  gap: 8px;
  padding: 16px;
}
.zukan-album {
  position: relative;
  width: min(94vw, 1100px);
  height: min(72vh, 680px);
  display: flex;
  background: #f6efe0; /* album.webp 受領までのフォールバック */
  background-size: cover;
  background-position: center;
  border: 10px solid #6b4a2b;
  border-radius: 14px;
  box-shadow: 0 10px 30px rgba(0, 0, 0, 0.25);
}
.zukan-album::before {
  content: '';
  position: absolute;
  left: 50%;
  top: 0;
  bottom: 0;
  width: 26px;
  transform: translateX(-50%);
  background: linear-gradient(
    to right,
    rgba(0, 0, 0, 0) 0%,
    rgba(0, 0, 0, 0.16) 50%,
    rgba(0, 0, 0, 0) 100%
  );
  pointer-events: none;
}
.zukan-page {
  flex: 1;
  display: grid;
  grid-template-columns: 1fr 1fr;
  grid-template-rows: 1fr 1fr;
  gap: 12px;
  padding: 5% 6%;
}
.zukan-slot {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  gap: 6px;
  background: none;
  border: none;
  padding: 0;
  font-family: inherit;
}
.zukan-unlocked {
  cursor: pointer;
}
.zukan-unlocked:active {
  transform: scale(0.96);
}
.zukan-photo {
  width: 100%;
  height: 68%;
  background: #fff;
  padding: 6px;
  border-radius: 4px;
  box-shadow: 2px 3px 8px rgba(0, 0, 0, 0.25);
  overflow: hidden;
}
.zukan-photo img {
  width: 100%;
  height: 100%;
  object-fit: cover;
}
.zukan-locked .zukan-photo img {
  filter: blur(10px) brightness(0.35) grayscale(1);
}
.zukan-name {
  font-size: 20px;
  color: #4a3b28;
}
.zukan-stamps {
  display: flex;
  gap: 3px;
  font-size: 18px;
}
.stamp {
  color: #cfc5ad;
}
.stamp.on {
  color: #f5a623;
}
.zukan-tabs {
  position: absolute;
  right: 0;
  top: 5%;
  transform: translateX(72%);
  display: flex;
  flex-direction: column;
  gap: 8px;
}
.zukan-tab {
  font-family: inherit;
  font-size: 17px;
  writing-mode: vertical-rl;
  padding: 14px 5px;
  border: 2px solid #b89a6a;
  border-left: none;
  border-radius: 0 10px 10px 0;
  background: #fff6df;
  color: #6b4a2b;
}
.zukan-tab.active {
  background: #ffd43b;
  font-weight: bold;
}
.zukan-nav {
  position: absolute;
  top: 50%;
  transform: translateY(-50%);
  width: 72px;
  height: 72px;
  font-size: 36px;
  border-radius: 50%;
  border: 3px solid var(--blue-dark);
  background: rgba(255, 255, 255, 0.85);
  color: var(--blue-dark);
  z-index: 3;
}
.zukan-prev {
  left: 10px;
}
.zukan-next {
  right: 10px;
}
.zukan-nav:disabled {
  opacity: 0.25;
}
.zukan-counter {
  position: absolute;
  right: 24px;
  bottom: 24px;
  font-size: 22px;
  color: var(--blue-dark);
  background: rgba(255, 255, 255, 0.8);
  padding: 6px 14px;
  border-radius: 12px;
}
.zukan-turning .zukan-page {
  animation: zukan-flip 0.25s ease;
}
@keyframes zukan-flip {
  from {
    opacity: 0;
    transform: translateX(14px);
  }
  to {
    opacity: 1;
    transform: none;
  }
}
.zukan-detail .photo-card img {
  max-height: 40vh;
}
.zukan-detail-name {
  font-size: 44px;
  color: var(--blue-dark);
}
.zukan-detail-formal {
  font-size: 20px;
  color: #777;
}
.zukan-detail-desc {
  font-size: 26px;
  line-height: 1.7;
  max-width: 78vw;
  color: #333;
  text-align: center;
}
```

- [ ] **Step 2: テストのリグレッションがないことを確認する**

Run: `npm test`
Expected: 全件 PASS

- [ ] **Step 3: 目視確認(ヘッドレスChrome)**

```bash
npm run dev &   # ポートは出力を確認
```

ヘッドレス Chrome + CDP のスクリーンショットスクリプト(過去に使った
`.superpowers-visual/` 保存パターン)で以下を確認する。localStorage に
`train-quiz-zukan` を仕込んでから撮る(例: `e5-hayabusa: 5, komachi: 2`):

1. ゲーム選択 → でんしゃずかんカードが出る
2. 図鑑1見開き目: 解禁1件(名前つき)、未解禁(ぼかし+スタンプ2点灯)、空き枠
3. タブで「ふつうでんしゃ」へジャンプ
4. 拡大カード(写真・なまえ・せつめい)
5. iPad解像度(1024x768 論理)でレイアウト崩れがないこと

ずれ(タブのはみ出し・スタンプの折返し等)があれば CSS の数値を調整する。
確認後、dev サーバーは停止する。

- [ ] **Step 4: コミット**

```bash
git add src/styles.css
git commit -m "style: add zukan album styling with CSS fallback pages"
```

---

### Task 8: アセット統合(album.webp / cover.webp)と仕上げ

**前提:** ユーザーが Gemini で生成した2枚(アルバム見開き背景・表紙アイコン)を
受け取ってから行う。置き場所はユーザーに確認(過去は `~/Downloads` や
プロジェクト直下の一時フォルダに置かれた)。

**Files:**
- Create: `public/images/zukan/album.webp`, `public/images/zukan/cover.webp`
- Modify: `src/styles.css`(ページ領域の % を実画像に合わせて微調整)

**Interfaces:**
- Consumes: Task 5 の `asset('images/zukan/album.webp')` インライン背景、
  gameSelect の `images/zukan/cover.webp` アイコン(どちらも配置するだけで有効になる)

- [ ] **Step 1: 画像を変換して配置する**

sharp はリポジトリの devDependency を使う(リポジトリルートで実行)。
一時スクリプト例(スクラッチパッドに作成、実行後削除):

```js
import sharp from 'sharp';
// アルバム背景: 横長のまま幅2048に縮小
await sharp('<受領した見開き画像>')
  .resize({ width: 2048, withoutEnlargement: true })
  .webp({ quality: 82 })
  .toFile('public/images/zukan/album.webp');
// 表紙アイコン: 幅600で十分(カード表示は width300px)
await sharp('<受領した表紙画像>')
  .resize({ width: 600, withoutEnlargement: true })
  .webp({ quality: 82 })
  .toFile('public/images/zukan/cover.webp');
```

表紙画像の背景が白でカードに馴染まない場合は、過去の BFS フラッドフィルの
背景除去スクリプト(白背景→透過)を再利用する。

- [ ] **Step 2: 実画像に合わせて位置を微調整する**

dev サーバー+スクリーンショットで、写真スロットがアルバムのページ面に
収まっているか確認する。ずれていたら `.zukan-page` の `padding` と
`.zukan-album` の `height` / `border` を調整する(画像側に革縁が描かれて
いる場合は CSS の `border` を外す)。

- [ ] **Step 3: PWA precache を確認する**

`vite.config.ts` の globPatterns に webp が含まれていることを確認したうえで:

```bash
npm run build
grep -o 'images/zukan/[a-z]*\.webp' dist/sw.js | sort -u
```

Expected: `album.webp` と `cover.webp` の両方が precache manifest に載る。

- [ ] **Step 4: 全体テスト・コミット**

Run: `npm test && npm run validate`

```bash
git add public/images/zukan src/styles.css
git commit -m "feat: integrate zukan album background and cover art"
```

---

### Task 9: マージとデプロイ

- [ ] **Step 1:** feature ブランチの全テスト・build・validate を最終確認する
- [ ] **Step 2:** `main` へマージ(fast-forward でよい)し、push する
      (push で GitHub Actions が Pages に自動デプロイ)
- [ ] **Step 3:** `gh run watch` でデプロイ成功を確認し、公開URLで
      図鑑の動作(オフライン含む)をスポットチェックする
